# `py-oineus`: Python bindings for the `oineus` library

This package can be used to install the Python bindings for the
[`oineus` library for calculating persistent homology](https://github.com/anigmetov/oineus/).
